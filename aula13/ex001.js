var count = 0;
console.log('Agora vamos fazer o while.');
while (count < 6) {
    console.log(`Count ${count}`);
    count++;
}
count = 0;
console.log('Agora vamos fazer o do .. while.');
do {
    console.log(`Count ${count}`);
    count++;
} while (count < 6);